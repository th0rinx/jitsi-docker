# =========================
# JITSI - .env (mínimo funcional con NPM externo)
# Copiar como /opt/jitsi/.env
# =========================

# Dominio público (el que apunta a NPM)
DOMAIN=jitsi.gpowstack.com

# El esquema público (NPM termina TLS)
SCHEME=https

# Zona horaria
TZ=America/Argentina/Buenos_Aires

# Versión de imágenes (la misma para todos los servicios)
VERSION=stable-10655

# Puerto HTTP interno en el VPS (NPM hará proxy a este puerto)
JITSI_HTTP_PORT=8080

# -------------------------
# JVB (Media / WebRTC)
# -------------------------

# Puerto UDP de media (abrir en firewall)
JVB_PORT=10000

# IP pública del VPS donde corre Jitsi/JVB (NO la del NPM)
JVB_ADVERTISE_IPS=184.107.171.10

# Fallback TCP (recomendado abrir 4443/tcp)
JVB_TCP_PORT=4443

# Límite de memoria recomendado
VIDEOBRIDGE_MAX_MEMORY=2048m

# -------------------------
# Auth (opcional)
# -------------------------
# 0 = sin auth (cualquiera entra por link)
# 1 = auth habilitado (solo usuarios registrados crean salas; guests si ENABLE_GUESTS=1)
ENABLE_AUTH=0
ENABLE_GUESTS=1
AUTH_TYPE=internal

# -------------------------
# Passwords XMPP (OBLIGATORIOS)
# Generá valores fuertes (ej: openssl rand -hex 16)
# -------------------------
JVB_AUTH_PASSWORD=CHANGE_ME_JVB_AUTH_PASSWORD
JICOFO_AUTH_PASSWORD=CHANGE_ME_JICOFO_AUTH_PASSWORD
JIBRI_XMPP_PASSWORD=CHANGE_ME_JIBRI_XMPP_PASSWORD
JIGASI_XMPP_PASSWORD=CHANGE_ME_JIGASI_XMPP_PASSWORD
JIBRI_RECORDER_PASSWORD=CHANGE_ME_JIBRI_RECORDER_PASSWORD
